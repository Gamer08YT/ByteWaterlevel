# OTA

The Firmware of ByteLevel brings two Types of OTA.

### Update Server



### Flash over OTA

You can flash the Board using the Builtin ArduinoOTA Server, if disabled, you need to enable this Function in the WebGUI.

If you enabled the Auth Function, you need to enter the Admin Password when flashing via OTA. 